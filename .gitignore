/target
samples/queue
*.o

# The BPF skeleton, bindings and vmlinux.h are generated by BpfBuilder into
# OUT_DIR (under /target) at build time, so nothing extra to ignore here. The
# maintained BPF sources live in src/bpf and are tracked.

# Vim swap files
*.swp
*.swo
*.swn
